apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: ecommerce-ai
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 8080
      name: http
